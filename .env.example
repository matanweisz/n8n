# =============================================================================
# N8N SIMPLIFIED ENVIRONMENT CONFIGURATION
# =============================================================================

# =============================================================================
# SECURITY KEYS - GENERATE THESE VALUES!
# =============================================================================
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=your-secure-postgres-password-here

# Generate with: openssl rand -hex 16
N8N_ENCRYPTION_KEY=your-n8n-encryption-key-here

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
POSTGRES_DB=n8n
POSTGRES_USER=n8n

# =============================================================================
# N8N ALB/PRODUCTION CONFIGURATION
# =============================================================================
# Domain configuration for ALB routing
N8N_HOST=n8n.matanweisz.xyz
N8N_PROTOCOL=https

# Webhook configuration - CRITICAL for ALB setup
# This tells n8n to generate webhook URLs using HTTPS and the correct domain
WEBHOOK_URL=https://n8n.matanweisz.xyz

# Editor/UI configuration
N8N_EDITOR_BASE_URL=https://n8n.matanweisz.xyz

# Security settings for production
N8N_SECURE_COOKIE=true

# Logging configuration
N8N_LOG_LEVEL=info
